#include <stdio.h>

int main() {
    int my_age = 43;

    // Outputs the value of my_age which is 43
    printf("%d\n", my_age);
    // Outputs the memory address of my_age
    printf("%p\n", &my_age); 

    return 0;
}